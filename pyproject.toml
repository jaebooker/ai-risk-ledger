[project]
name = "ai-risk-ledger"
version = "1.0.0"
description = "Aggregate AI-risk forecasts from Metaculus, Manifold, Polymarket and Kalshi"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
